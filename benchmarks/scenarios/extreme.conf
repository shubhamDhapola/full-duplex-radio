# Deliberately past the point of usability, to find where the pipeline breaks
# rather than assuming it degrades gracefully.
name = extreme
base_delay_ms = 100
jitter_ms = 100
jitter_shape = pareto
burst_loss_percent = 10
burst_mean_length = 8
reorder_percent = 3
duplicate_percent = 1
